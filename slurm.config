singularity.enabled = true
process {
    withName:fastqc {
        executor='slurm'
        queue="${params.queue}"
        cpus=8
        mem='20 GB'
        time='4h'
        jobName={ "$task.name" }
        container = "${params.image_folder}fastqc-0.11.9.sif"
        containerOptions = "-B ${params.project_folder}:/workdir -B ${params.raw_data}:/raw_data"
    }
}

